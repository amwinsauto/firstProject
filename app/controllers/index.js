function doClick(e) {  
    alert($.label.text);
}

function onClick(e) {
//	Alloy.createController("index2").getView().open();
//	Alloy.createController("test");
//	Alloy.createController("test2");
}

// Ti.Gesture.addEventListener('orientationchange', function(e) {
	// Ti.API.debug("Height: " + $.table.size.height);
// });
// 
// $.index.addEventListener('postlayout', function(e) {
	// Ti.API.debug("Height: " + $.table.size.height);
// });

$.index.open();
